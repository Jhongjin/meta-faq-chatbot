[start]
cmd = "python -m uvicorn app:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHONPATH = "/app"

[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmd = "pip install -r requirements.txt"
